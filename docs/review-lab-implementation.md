# 测试复盘：隔离实现与交付状态

## 最新状态：2026-09-12 历史小样本闭环已发布

已部署 release-20260912-140146-df512b27，后端与已安装 VSIX 身份匹配，需 Reload Window。
新增“测试复盘 · 历史闭环结果”入口，列出独立目录中的运行供选择。
本次用本地周三至周五 9 行历史行情，完成一笔模拟往返和本地学习总结。
GET /api/v1/review-lab/runs 和 /runs/{run_id} 只读独立结果，不启动计算。
脚本 scripts/run_review_lab.py 为此次固定日期的手动诊断入口，不是生产调度器。
后续若换日期或股票，必须修改明确输入并重新验证，不自动拉取更多数据。
学习不是 AI 训练，七个上游项目尚未实际执行。以下第一阶段的未发布记录仅为历史。

## 已实现（第一阶段，尚未安装发布）

- 独立命令与侧栏入口，打开独立无脚本 Webview，不替换主终端页面。
- 七个候选项目分别展示，按日期读取独立产物，无数据不补采、不虚构结果。
- 只在用户打开页面时读取文件；每个文件最多 64 KiB，串行读取。
- 结果位于 VS Code extension globalStorage/review-lab/results/YYYY-MM-DD/，
  文件名为 owner__repo.json。不创建或访问生产数据库，不获取数据接口或 AI 凭据。
- 校验日期、项目身份、版本、reference_only、生成方式、来源版本、输入截止和局限说明；
  无效数据仅显示错误，禁止自动替换为其他日期。HTML 转义和 CSP 禁止结果执行脚本。
- 改动前 Git 备份：codex/backup-before-review-lab-20260912，提交 7242ae2。
  这是已跟踪源码备份，不含运行数据、密钥、未跟踪产物。

## 尚未实现：后台运行（不得把第一阶段当作自动复盘已完成）

2026-09-12 增量：已增加手动命令 `quantai.reviewLabSimulate` 与合成模拟器。
无需等待 19:00，内存运行，不写正式每日结果。CLI 演示生成器在新的临时目录写出
四种场景的 JSON/HTML，全部标记 synthetic_simulation。此模式不接入上游或真实 AI，
空闲状态为注入信号，只验证门禁分支，不证明生产租约与抢占能力。

1. 核验七个项目的许可证、固定提交和实际可运行能力。框架不代表盈利能力。
   不安装执行第三方交易/券商入口；独立改写观察方法须标注 local_observation，
   真正上游回放才标注 upstream_replay，独立模型生成才标注 isolated_ai。
2. 独立低优先级工作进程，独立结果目录、输入快照和日志，禁止继承生产密钥。
   不写入生产决策、学习记忆、交易账本，不调用交易、通知或强制补采。
3. 北京时间 19:00–19:40 仅尝试一次。生产任一流水线、补采、AI、交易或维护活动
   均优先；状态未知一律拒绝运行。仅看 tasks.running_tasks 不够，手动流水线也须覆盖。
   空闲检测和任务启动存在竞争窗口，需要主流程优先的可撤销租约，不能只轮询一次。
4. 输入只读离线副本；不得直接扫描生产 SQLite。快照生成也须纳入资源预算，
   不能把复制大数据库造成的 IO 竞争忽略。无合格快照则 no_data，不现场抢配额。
5. 不共用现有付费 AI 队列。未配置独立 AI 预算时明确降级为方法观察，不能称 AI 已学习。
6. 每项目报告记录数据日期、输入指纹、源码版本、实际运行方式、事实观察、
   证据缺口与失败原因；日期与交易日期分开，周末不生成伪交易日结果。
7. 验证时段、未知/忙碌拒绝、中途撤销、多窗口互斥、崩溃租约恢复、重入幂等、
   无生产写入/外呼、超时进程退出，再启用自动调度。

## 发布约束

本阶段不重启后端、不安装 VSIX、不创建系统或 Codex 定时任务。
完整自动复盘尚未实现，保持关闭。后续验收通过后使用现有统一部署流程打包，
并在主系统空闲时切换；不得单独覆盖已安装 release 身份文件。
