# Luna：生产候选数据缺口补齐实施与验收

状态：方案，实施未完成。作为 luna-tushare-completion-plan-20260906.md 的生产验收增补，遵守根目录三份规范，保留全部已有修改。仅使用现有会员权限；不改选股公式、65分研究门槛、Top N、交易和学习规则。不触发交易。

## 基线与已确认原因

固定生产 run_id=4694431b97f948c1bf6fd85a5645f9ae，300只，行情截止2026-09-04。原始产物 test-reports/production-pipeline-20260906.json。

- 资金流295/300，positive182、negative113、missing5。
- 基本面available151/300（50.33%），不是“四类报表各8期”的完整率。缓存完整财报命中108/300；不得混用这些指标。
- 资金流缺失：002909.SZ(61)、300418.SZ(62)、002886.SZ(62)、600095.SH(62.8)、600378.SH(64)。查询时生产资金流历史均0行，未进入联网补采。
- 基本面缺失149只：148只最终排名分低于65且未联网补采；600681.SH尝试过，报价成功、财务不可用，错误只有“Tushare 财务指标获取失败: ”。具体异常类型未确认。
- 补齐函数await完成后才进入AI预选，未发现本轮AI抢先执行的证据。排队、网络超时可影响个别请求，但不是148只未安排补采的解释。
- 本轮报价75/75成功不代表财务75/75成功。流程errors=[]不代表组件无失败。
- 耗时：服务452.4秒，客户端474.77秒；补齐167.623秒、预选28.67秒、Deep143.58秒、终审19.662秒。
- 同日研究追加记录不直接证明重复学习；run_id存在strategy_decision.analysis_json中。只读核验学习消费，未证明缺陷前不得增加schema或更改学习规则。

## A：锁定基线与逐组件任务账本（P0）

从analysis_json.run_id精确导出固定300只，不以当天最后300条替代批次筛选。对资金流与四类财报分别保存请求窗口、目标日期、缓存有效期数、是否调度、原因、来源、尝试次数和耗时。保存生产只读基线和一致性SQLite备份；隔离试验使用备份，不覆盖生产数据库。

## B：拆开数据补采与研究资格（P0）

复用data_completion_service及backfill_research_data，为固定300只及持仓建立组件缺口队列。低于65分或不在80只即时队列中的股票也必须有缓存结果或补采状态。65分/80只限制继续用于既有昂贵研究路径，不控制基础资金流/财报后台回填。

优先持仓及Deep候选，其余候选分批完成。所有数据有效者复用缓存；仅补缺期、新披露及修订。禁止通过把min_ranking_score改0或扩大全市场Deep名额来实现补采。优先复用现有调度器和checkpoint，避免第二套任务服务。

正式接入现有任务调度：新候选生成后入队；前台预算结束时持久化待补任务，后台有限恢复。进程重启后继续；每个代码/组件/目标日/窗口幂等，成功组件不重复全量下载。不能仅交付一次性人工脚本而称长期闭环完成。

## C：可解释失败与有界等待（P0）

修复600681.SH路径异常丢失：至少保存异常类型、脱敏repr、endpoint、是否实际发送、排队耗时、网络耗时、总耗时、尝试次数和最终状态；空异常文本也应保留TimeoutError等类型。

区分not_scheduled、pending、partial、timeout、rate_limited、permission_denied、empty、invalid、stale、not_yet_published、available。限流有限等待，权限拒绝不当限流重试，全部受总deadline和最大尝试次数限制。复用共享预算和在途去重，不自行放宽现有前台30秒预算。后台预算先沿用已有方案并以隔离测试确认；到期持久化延期，不死等。

## D：挂接与可观测性（P0）

补采完成后重新读取已提交的数据，再构造AI输入；本轮读取数据截止点之后才到达的数据留给下一轮，并明确pending，不声称已送给本轮AI。任一组件失败不得覆盖其他有效组件；检查新获取的简版财务摘要是否覆盖了已有8期完整报表，必要时按组件保留。

输出分别包含：资金流有效覆盖、基本面摘要available覆盖、四类报表各8个独立报告期完整率、待补数量、未调度数量、失败分类。每项明确分母和run_id。

AI消费验收读取实际构造并提交的输入：验证报告日期、单位和关键数值、资金流日期/数值及输入hash。available和计数不能单独证明完整内容被消费。缺失不得伪装正资金流或健康基本面。

## E：测试及隔离实跑（P0）

1. 固定300只含低分/80只外样本：每只均有组件状态，静默遗漏0；固定输入下选股公式和交易判定不变。
2. 模拟真实阻塞、排队到期、限流、权限拒绝、空异常文本、部分成功：证明有界等待、无同请求重叠、失败详情关闭重开临时DB后可读。不得生产注入故障。
3. 真实验证上述5只资金流和600681.SH财务路径；解释仍失败的确切原因，不能用报价成功代替财务成功。
4. 冷缓存/暖缓存按同一固定300只和日期比较；恢复checkpoint不重复补采，跨日期更新正确。
5. 隔离全流程execute_paper_trades=False：验证20预选、5 Deep及终审实际完成情况；补齐后评级自然变化可接受，规则不变。
6. 执行相关回归；有变更或失败再扩大测试。报告未测试项，不能沿用此前746通过替代本次验证。

## F：生产落地（依赖隔离验收）

使用现有生产部署脚本及运行契约，非交易窗口、确认无冲突任务后切换。用户已授权本项正常实施及生产落地；若进程权限确实不足，记录具体阻断，不绕过契约。

使用既有回填工具的显式生产写入模式分批填充生产数据表，先备份并核验目标路径；禁止把隔离DB整体覆盖到生产（会覆盖决策、学习和账本）。生产回填必须实际执行并验证，不能仅重启源码就标完成。新运行以无交易方式读取生产补齐结果，交付新run_id及实际消费证据。

## 交付与完成标准

- test-reports下固定300只前后逐组件JSON/CSV、请求和失败记录、冷暖耗时、真实AI输入校验证据、测试报告、生产新run结果。
- 所有股票的缓存核查/任务状态覆盖100%，未解释遗漏0；可得数据实际挂接，不可得数据有可核验原因和后续策略。不强求伪造100%数据成功率。
- 生产补齐、调度接入、隔离验收、生产验收分别列状态，未完成不得勾选。
- 附最小修改文件清单及回滚步骤；交易、持仓、现金和学习数据不因回填被覆盖。

## 可直接交给Luna的执行指令

按本方案和根目录dev-plan.md、project-structure.md、code-guideline.md实施。保留已有修改，使用现有2000积分权限，不改选股/交易/学习规则。先固定生产基线，再完成组件补采队列、错误详情、后台恢复和AI挂接，隔离验证后分批回填生产并完成无交易复跑。常规实现无需中途确认；交付逐只原因、前后覆盖率、耗时、AI实际输入证据、测试及剩余问题。未完成项不得标完成。
