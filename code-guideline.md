# 2026-09-06 Luna 两轮复核返工规范（待实施）

本节与dev-plan.md顶部返工计划共同约束本轮；保留下方历史规范。只修已确认六问题及必要验收，不修改研究/交易/学习规则，不把补采覆盖当交易绩效。本轮不进行生产部署或生产写入。

## 数据与兼容性

- Python沿用项目命名与类型风格，新增字段snake_case；优先扩展既有结构和函数，旧调用参数保留兼容默认，但缺少必要目标日期时不得默认为新鲜。
- 完整历史`statements`与最新摘要分开保留；禁止available=true就整体替换原fundamental。来源、报告期、披露日、抓取时间跟随各组件，合并不得把一源来源覆盖成另一源。保留旧消费者必需的字段形态，新增字段显式版本化。
- 四类财报分别按独立报告期去重，修订选择依据已披露且as-of可知的版本；保留原始值、单位及修订元数据。不得用重复修订记录凑8期，不能以0补缺失或无依据换算单位。
- 完整性、可用性、新鲜度独立：例如8期齐全但最新披露未核验，不等于当期有效。已存数据不能因一个源失败被删除；完成判断必须取提交后读回结果，不取本次响应行数。
- 新披露核验复用现有权限可调用接口，缓存有效时避免每轮全表下载。检查间隔优先沿用已有配置；缺失时使用明确、有上限且可测试的刷新配置，不无限缓存，不按季报自然年龄任意判风险。

## 日期及AI输入截止

- 本轮`target_trade_date`来自已核验的最近完成交易日，另保存calendar_source/verified及as_of截止；不能以个股旧K线替代。日线资金流不是实时流。
- 保存历史direction（positive/negative/missing/invalid）及独立freshness/status。stale方向可作历史展示，不可自动作为当期可信资金流；沿用现有证据不足/因果性守卫处理，不新设买卖阈值或豁免。
- 停牌、未披露、权限不足与网络失败分开解释；无证据不得推断停牌。日期未知时明确unknown，禁止使用抓取时间冒充行情日期。
- 输入冻结后晚到结果仅留给后续研究，不回写历史输入。隔离回放过滤未来披露/修订；不能用今天抓到的未来已知版本伪装过去已知数据。
- 区分DB完整、候选挂接完整与模型实际提交。保留旧input_sha256的摘要语义并标注hash_scope，新增完整请求指纹覆盖实际system/user/messages及工具schema等模型可见字段，使用确定性序列化。认证头、token等不进入提示或审计文件。
- 验证发送边界实际字段、报告日期、单位、关键数值；如果仅提交选定指标，列明实际字段集，不宣称完整原始财报被分析。禁止为验收修改预选提示、研究名额或评分规则。

## 任务身份、恢复与并发

- 候选run订阅与数据工作身份分离。工作身份至少含隔离/生产数据库身份、代码、组件、目标日或as-of、窗口及数据契约版本；同一工作可服务多run，不能跨环境误复用checkpoint。
- 默认批次选择须有明确成功状态和排序依据，不按行数猜最新；缺可靠成功记录须明确报无法验证。显式run不受最新decision_date过滤，空集/未知run不能报completed。
- 优先持仓、已知Deep候选，再其余候选；所有候选均登记，不受65分/80只即时门槛截断。额外持仓单独计数，不挤占候选配额。
- 复用checkpoint记录pending/running/deferred/completed/terminal_failure/superseded及next_retry_at、lease_owner/expiry；名字可兼容现有状态，但语义须可映射。数据状态与任务状态分开，stale不等于任务成功。
- 状态认领/完成须原子和幂等；旧worker不得覆盖新lease结果。提交和checkpoint存在中断窗口时，恢复必须先读缓存避免重复采集；新增/修改成功组件不重复全量下载。
- 启动、定时和CLI复用同一服务层，不从API复制补采循环。跨run/跨日显式恢复或终结旧待补，不能因换checkpoint分区丢失工作。

## 有界等待、错误及审计

- 所有等待使用单调时钟总deadline：队列、限流退避、分页、网络及一次重试都计入。前台30秒及更严格原子预算保持不变；后台切片按dev-plan默认60秒或已有更严格预算实现并隔离实测。
- 仅可重试错误有限重试；权限失败为终止/配置变更待恢复，不盲等。deadline到期延期并退出worker；超时线程仍在途时禁止同key重叠重试，不声称已被取消。
- 逐尝试至少记录组件、endpoint、request_id、队列/发送时间、sent、queue_seconds、network_seconds、total_seconds、attempt_index、结果类型、重试原因及剩余预算。由请求层产生事实，不用source推算次数；共享缓存命中与实际发送分开计数。
- 空异常保存异常类型；在任何日志、JSON/CSV及checkpoint写出前统一脱敏token、Authorization、密码及敏感URL参数。先脱敏再截断；只用合成敏感值做测试，不读取真实密钥作样本。
- 将not_scheduled、pending、partial、timeout、rate_limited、permission_denied、empty、invalid、stale、not_yet_published、freshness_unknown、available映射清楚。无数据原因无法证实时写unknown，不能称必然限流或必然源端无数据。

## 测试与交付

- 先复现后修复：覆盖覆写、旧K线与旧流、错误run选择、未自动入队恢复、过期8期财报、假请求次数/无脱敏六项；每项都绑定测试和真实/隔离证据。
- 使用实际阻塞及受控超时，不仅立即raise；计时断言给有限调度容差，检查退出时间和残留inflight。恢复测试使用隔离子进程，不终止生产进程。
- 隔离DB、缓存、输出和单例明确注入，关闭交易、通知和学习写入；测试前后核验受保护表内容及账户金额，不能只核对行数。全市场结果数据变动允许，交易账本和学习表不得变化。
- 回归证明固定有效输入下评分排序/交易判定不变；仅证据状态纠正引起的差异逐只标注。冷暖跑使用同集合/目标/窗口并分别保存，不覆盖历史证据。
- 验收分别给代码修复、隔离补采、后台恢复、隔离AI、生产部署、生产AI六类状态。生产两项本轮未授权，不用此前报告/注册状态替代实跑。每项给本轮命令、时间、源码版本范围、结果文件及未完成原因。
- 不把所有外部数据成功作为完成必要条件；必须做到状态登记无遗漏、原因不伪造、可得数据不被覆盖、恢复有限且可验证。尚未实测的权限/调度首次触发/模型输入必须明确未完成。

---

# Adaptive Investment 交易执行分层改造规范

## 2026-09-06 生产补齐验收补充

本轮状态：未改变选股/交易/学习规则；300只生产补采已完成逐组件账本（297 complete、3 stale），748项回归通过。生产后端已切换并完成新进程无交易AI复跑，20:30调度已注册；3只 stale 继续保持不可伪造。

- 基础数据补采范围独立于65分研究资格；评分、Top N及交易规则保持不变。
- 以analysis_json.run_id锁定批次；不得因表无独立run_id列就认定批次不可区分。
- 基本面摘要available与四类财报各8期完整率分开统计。
- 异常文本为空仍保存类型；区分未安排、排队到期、请求失败与部分成功。
- 隔离成功不代表生产回填完成；禁止以隔离数据库整体覆盖生产数据库。
- 生产回填需备份、幂等、逐组件证据；实际AI输入验证与交易零增量分别验收。

## 2026-09-05 授权补充

本轮允许依据隔离验证调整模拟探索，但不得改变实盘规则、选股公式或将终审否决改作批准。不以交易次数证明学习有效。优先修复证据缺口；结构化提示的实际值、类型与期数必须验收，不能只校验声明计数。生产未重启、接口未实测、隔离未通过的条目不得标完成。保留本文件历史规则，新增探索分支必须显式paper范围并与旧规则对照。

## 一、不可变约束

1. 本次不得修改选股池、技术评分、AI 预选、Deep 分析、最终复核、阈值、排序权重或 Top N。
2. `positive`、`negative`、`missing`、`invalid` 必须保持四态；`missing/invalid` 不得转换为 `positive`。
3. `negative` 永远不能触发 normal 或 probe 买入。
4. probe 只允许用于 paper trading；任何 live/broker 执行必须强制关闭。
5. 生产进程禁止 `--reload`；只能在非交易窗口通过版本、代码哈希、schema 和测试门禁后重启。
6. normal 与 Deep Buy 的现有卖出阈值保持不变；Deep Buy 只豁免软退出，硬止损和最长持仓期继续生效。

## 二、执行判定契约

执行层输入必须包含：`decision_id`、`stock_code`、`final_direction`、`pre_gate_direction`、`ai_score`、`deep_rating`、`flow_state`、`flow_sources`、`fallback_attempted`、`fallback_status`、`gate_reasons`、`non_flow_gates_passed`、报价时间、数据截止时间和行业。

执行层输出统一为：

- `normal`：原有买入规则全部成立，资金流为可信 `positive`。
- `probe`：`pre_gate_direction=buy`，原有分数和非资金流门槛全部达标，最终未买入的唯一原因是 flow `missing/invalid`，备用源已经尝试且没有任何可信来源返回 `negative`。
- `blocked`：资金流 `negative`、存在其他门槛失败、备用源未尝试、数据/报价过期、因果性不合格、仓位/行业超限或证据无法审计。

严禁用“最终 neutral + 分数较高”直接推导 probe；必须证明原始买入意图存在且唯一阻断项是资金流缺失。

## 三、买入规则

### Normal

- 完全沿用现有买入条件和目标仓位。
- 继续检查报价完整性、交易日、数据因果性、现金、单股仓位和行业市值上限。

### Probe

- 默认单笔使用账户净值 2%，任何配置不得超过 3%。
- 所有 probe 合计市值不得超过账户净值 5%。
- 每个交易日最多新增 1 笔，同一股票同时最多 1 个 probe 持仓。
- 不允许对亏损 probe 加仓，不允许用 probe 绕过行业 40% 上限。
- 成交价格、滑点、佣金、印花税和手数规则与 normal 共用同一实现。
- 拒绝时写入结构化原因，不得静默跳过。

## 四、Probe 生命周期与卖出规则

状态仅允许：`open → promoted`、`open → exited_negative`、`open → exited_unconfirmed`、`open → exited_stop_loss`、`open → exited_max_holding`。

- 硬止损：相对 probe 成本价达到 -4% 时，在下一个可成交窗口退出。
- 数据确认：入场后 3 个交易日内，若资金流恢复为可信 `positive` 且重新满足原 normal 买入条件，标记 `promoted`。
- 转正补仓：只允许补至原 normal 目标仓位，并重新检查现金、单股和行业敞口；不能追溯修改原成交。
- 明确转负：任一可信权威来源确认 `negative` 时，在下一个可成交窗口退出。
- 未获确认：第 3 个交易日结束仍为 `missing/invalid` 时退出，不以数据缺失长期占用仓位。
- 绝对上限：任何 probe 到第 5 个交易日必须退出，除非此前已成功 promoted。
- 报价缺失、停牌或跌停时保留待卖状态并后延，不得按成本价伪造成交。
- promoted 后完全转入 normal/Deep Buy 既有持仓规则，不再适用 probe 超时退出。

## 五、命名与文件组织

- 枚举使用 `ExecutionTier`、`FlowState`、`ProbeStatus`；值使用小写字符串以兼容 JSON/SQLite。
- 纯判定放在 `execution_policy.py`，不得在数据库循环中复制条件。
- 数据库层只负责读取、持久化和调用策略，不得重新计算选股分数。
- 所有新字段向后兼容：旧持仓默认 `execution_tier=normal`，不得被错误识别为 probe。
- 时间统一保存带时区 ISO 8601；期限使用交易日计数，不使用自然日差。

## 六、错误处理与审计

- 未知 flow 值按 `invalid` 处理并记录原始值。
- 备用源失败必须记录 source、错误类别、发生时间和是否耗尽重试。
- 执行输入缺字段时 fail-closed，写入 `paper_order_rejection`，不得抛错后继续部分成交。
- 同一 `decision_id + execution_tier + trading_date` 必须幂等，重复调度不能重复下单。
- 每次买入、转正、卖出都记录决策 ID、策略版本、执行 tier、触发规则、报价来源和数据截止时间。
- 连续弃权告警只报告数据质量，不得自动改变阈值、仓位或启用 live execution。

## 七、测试与验收要求

- 执行策略必须使用表驱动单测，覆盖四种 flow 状态、备用源结果、多个阻断原因和边界仓位。
- 必须加入回归快照，证明相同输入下候选顺序、技术分、AI 分、Deep 分、最终方向和 normal 交易结果与改造前一致。
- 必须验证 probe 的单笔 2%、硬上限 3%、组合 5%、每日 1 笔和行业 40% 上限。
- 必须验证 -4% 止损、negative 退出、3 日未确认退出、5 日上限、停牌/跌停延续卖单和重启后身份恢复。
- 必须验证 paper、backtest、ledger rebuild 的价格和费用口径不发生分叉。
- 必须验证每日账务恒等式：`equity_change = market_pnl + realized_pnl - fees + cash_flow_adjustments`，不平时阻断发布。
- 全量测试、编译检查、数据库迁移回滚演练和运行契约校验全部通过后才可采用新版本。

## 八、上线步骤与回滚条件

1. 先以 shadow 模式运行 3 个完整交易日：计算 probe 决策但不成交，对照记录候选数量、阻断原因和事后收益。
2. shadow 无选股回归、无重复下单、无账务差异后，仅对 paper trading 启用 2% probe。
3. 出现重复成交、执行层改变候选排序、行业/总敞口越界、账务不平或旧持仓被误标 probe 时立即关闭 probe 开关并回滚运行契约。
4. 不得因为连续零交易提高 probe 上限或降低原选股阈值；应优先修复数据覆盖和备用源质量。

## 2026-09-06 部署运行契约增补

- 生产服务必须由版本校验入口启动；`--reload` 保持关闭。
- 运行身份包含 release_id、artifact_hash、PID、进程启动时间、boot_id、代码根和数据身份；接口中的磁盘漂移只能作为告警，不能冒充运行版本变化。
- 端口存活、HTTP 200、计划任务启动返回成功或新PID均不能单独证明新代码生效；必须完成运行清单和行为接口验收。
- 旧进程身份按PID、创建时间、命令行、路径和祖先关系核验后才允许停止；陌生监听者必须失败并保留现场。
- 部署互斥锁跨会话有效；维护状态期间禁止新任务入场；清单或启动验证失败时熔断自动重启并记录恢复状态。
- 旧历史记录可以没有新增字段；新字段必须通过真实生产/隔离链路持久化、关闭重开读回验证。未跑完整AI时不得标记生产AI已验收。
- 生产服务必须从 `runtime/releases/<release-id>` 的独立源码副本启动；工作树只用于开发和生成发布物。
- 发布副本不得复制 `.venv`、生产数据库或可变缓存；任何数据/配置绝对路径必须显式保留并在部署验收中核对。
- 部署验收至少证明 active manifest、实际 `source_root`、artifact hash、PID/启动时间和数据库路径一致；未证明的生产切换不得标记完成。
- 部署前必须停止并验证旧启动器/看门狗进程树；只停止 8888 监听 PID 不足以证明旧代码不会再次拉起。
